import styled from "styled-components";
import _ from "lodash";

export default function Ul(info: Array<Object>) {
  debugger;
  return (
    <ul>
      {info.map((pkmn) => (
        <li>1</li>
      ))}
    </ul>
  );
}
